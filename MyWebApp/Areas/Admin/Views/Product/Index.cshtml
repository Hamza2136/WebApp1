@model MyApp.Models.ViewModel.ProductVM
@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row">
        <div class="col-6 text-primary p-4">
            <h2>List of Products</h2>
        </div>
        <div class="col-6 text-end p-4">
            <a class="btn btn-primary" asp-area="admin" asp-action="AddUpdate" asp-controller="product"><i class="bi bi-plus-square"></i>&nbspClick to Add Category</a>
        </div>
    </div>
</div>
<table id="mytable" class="table table-striped table-bordered w-100">
    <thead>
        <tr>
            <th>Product Name</th>
            <th>Description</th>
            <th>Price</th>
            <th>Category</th>
            <th>Image</th>
            <th>Action</th>
        </tr>
    </thead>
    @* <tbody>
        @foreach (var item in Model.products)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@item.Price</td>
                <td>@item.Category</td>
                <td role="group">
                    <a asp-action="AddUpdate" asp-controller="product" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i></a>
                    <a asp-action="Delete" asp-controller="product" asp-route-id="@item.Id"><i class="bi bi-trash"></i></a>
                </td>
            </tr>
        }

    </tbody> *@
</table>

@section scripts{
    <script src="~/js/AllProduct.js"></script>
}

